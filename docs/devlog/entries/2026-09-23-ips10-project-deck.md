# 2026-09-23 — IPS10 project companions and tactile deck

User feedback identified two losses in the task-workspace redesign: prominent usage data and the familiar creatures working in shared project spaces. The default view now restores both. Exact reported project names group live sessions into expandable project decks; unknown names remain separate. Each canonical creature keeps its brand color, has a distinct work/attention/rest state, and opens its own stable session ID on touch. Grouping expresses project co-presence, not an invented delegation edge. The existing task rail, child/background census, history and voice drawer remain in the detail flow.

Claude/Codex quota bars show used percentages and reset text. Missing values remain unavailable; zero is valid. Claude's stale flag retires only its own windows. Input/output token counts, tool calls and estimated cost are presented as the received daemon snapshot, explicitly separate from selected-session or project totals.

The user's Stream Deck direction is implemented with reusable 108×108 touch targets, pressed-state depth and a shared Blender-baked keycap. `design/ips10/keycap.py` creates a separate scene with token-based materials and restores the previous active scene; `keycap.png` is converted by `encode.py` to a 36 KiB flash-resident RGB565+A8 image. Brand geometry stays in the existing canonical masks. Ten project containers and ten creature seats are preallocated and reused without a frame-sized canvas or per-update growing containers. Only awaiting creatures animate.

Validation: actual native pointer press/release and detail navigation pass at 1280×800 and 800×1280. Regression cases cover session priority, reorder/removal, strict event attribution, wrapped history, filters, drawer bounds, offline/empty state, ten distinct projects, ten peers, unnamed projects, zero quota and stale-Claude/Codex independence. Both layouts were visually reviewed; physical IPS10 rotation remains unsupported. IPS35 native build and multi-session rendering also pass. `pnpm build`, `pnpm typecheck`, and 4,654 tests (305 files; two skipped) pass. Protocol generation causes no drift; all seven token mirrors and PNG-to-LVGL asset verification pass. Design lint remains at the prior 92 violations. Documentation/catalog checks pass.

Firmware application size is 5,471,830 / 6,291,456 bytes (87.0%). Runtime diagnostics identify this surface as `project-deck-v2` and include project count and overview mode. The identified IPS10 completed full USB flashing with all write hashes verified, then ran the new surface for the 90-second observation. Three live sessions formed two projects; UI updates advanced to 321. Update processing median was 6.617ms and maximum 37.853ms including startup (excluding display flush). Internal free heap settled at 78–79 KiB, minimum 73 KiB, largest block 36 KiB; no panic or reboot was observed. The serial lease was released and the Mac app reopened. The daemon then confirmed the same firmware epoch at uptime 109s, connected, fresh, and not stale. Speech recognition revalidation remains deferred.

The image SHA-256 is `814276bcb0083854acea5605b550ff3f6a6654ddd601724e5c66d14b15e779bc`; build epoch `1790131837` and pre-commit label `9efef312-dirty` correspond to implementation commit `206c85ef`. The first USB write stopped at 8.1% during a concurrent daemon ownership transition. The Mac app subsequently retained the TTY while its HTTP port changed, preventing the next preflight. Recovery suspended the current CLI daemon, briefly quit the port-holding Mac app, re-identified the P4/MAC/16MB flash, and wrote the complete image successfully. No unrelated process was killed.
