# 2026-09-24 — Verify store delivery and delayed macOS port handoff

Play Console confirms production 1.5.0 (21) active and available in 177 countries/regions. Elgato 1.4 is Ready to publish; the processed package installs and renders four dial regions, including z.ai labels. Physical dial interactions and multi-provider cycling remain pending in #349. When only Codex usage is available, the released source deliberately permits both usage dials to show it. Ulanzi 1.4.0 remains under review. Apple statuses remain owner-reported: iOS 1.5.0 released, macOS 1.5.0 under review.

For #370, tested published npm 1.4.1 and 1.4.2 against the same delivered macOS 1.3.2 (6801) app, with the existing connected devices. Both exact npm installations pass the native SQLite diagnostic under Node 26.5.0. After quitting the Swift app while it owned 9120, both exhausted their 20-second startup wait: 1.4.1 (`41b62526b121`) bound 9122; 1.4.2 (`6983b2aae908`) bound 9121. A later real socket bind probe found 9120 available, while the running Node daemon remained on its fallback. This establishes that the symptom predates 1.4.2; it does not establish the OS cause.

The #374 candidate retried the real listener and bound 9120 after 25.168 seconds. Rebuilt the exact source `0b3744bc17830c4aa17307b9c38cea7d39876364`, then repeated the delivered-app handoff: final build `cec51f0a25a6` bound 9120 after 25.638 seconds, without fallback. IPS10 and TRMNL fresh device reports each carried four sessions and two processing sessions. The earlier candidate also coexisted with the delivered app on 9120. No npm release or tag was created.

Stopping Node also allowed Swift to recover 9120, sometimes after its existing 120-second recent-bind-failure cooldown. Live Codex work appeared in the Swift Dashboard and the Stream Deck+ RUNNING tile. Swift-only BLE modules reported powered-on wait timeouts, so this is not an all-module acceptance pass; Node coexistence displayed both BLE devices connected. The test used the existing Codex work as live traffic and did not reinstall hooks or flash firmware. Temporary builds and raw diagnostics stay local. The shared voice-development runtime is restored after testing; the npm publication gate remains open.
