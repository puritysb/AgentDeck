# 2026-09-23 — IPS10 persistent Usage, readable details and fair roster paging

Closed the gaps found in the post-deployment visual audit. Known Usage quota rings now remain visible in both aquarium and detail views. Unknown collaboration cards collapse; the activity region measures its content and gives the remaining height to recent results/history. Narrow session titles are constrained to one line. Portrait previews use a horizontal session rail above the detail surface, compact filters and a covering background; physical IPS10 remains fixed landscape.

Project widths reflect the number of received peers. Up to three creatures remain filled across cohort boundaries, with up to two attention agents pinned and remaining slots rotating every eight seconds. Visible/total labels make the rotation explicit. Stable per-project numbers bind creatures to their activity rows; tool icons describe reported work, and a static branch/worker badge represents reported child counts, never inferred cross-session relationships. Recent results follow available activity instead of being anchored to the screen bottom. Project pages advance every 12 seconds. Voice readiness now requires both a connected daemon and an authenticated, error-free gateway; the local wake detector's readiness remains distinct. The voice drawer is narrower and its controls size relative to their parent.

Both Node and Swift serial projections now rotate oversubscribed rosters every 60 seconds. At most ten details are sent in each frame; up to three attention rows stay pinned, and the remaining capacity fairly visits all other alive rows, including excess attention. `bridge/src/ips10-roster.ts` is the policy origin; `bridge/generate-ips10-roster.mjs` emits the pure Swift index-selection kernel. Tests cover 1,000 sessions, excess attention, dead sessions, phase stability and executable Swift parity. IPS10-local `rosterRotating` is explicit true/false and absent means legacy priority-only behavior. A selected detail leaving a roster page is preserved and labeled as a saved detail; it never silently changes to the next session. Runtime diagnostics now report actual quota visibility/window count and roster metadata.

Validation: build and typecheck passed; the full JS suite passed 4,656 tests with one existing live-port occupancy test failing when a different process held the supposedly empty port. That unchanged seven-test file passed in isolation. Protocol generation has no drift. The macOS app build passed (without launching/replacing the app), and the generated selection kernel passed executable Swift/Node parity. Native IPS10 interaction checks passed landscape and portrait, including persistent detail Usage, saved-detail identity across a simulated host page, readiness gating, missing/zero/stale quota, four providers, cohorts, selection, history attribution and drawer bounds. Token mirrors and documentation/catalog checks passed. Design lint reports the same 92 source findings plus 39 findings in the ignored Xcode-built HTML copy.

Deployment preserves the installed npm 1.4.2 daemon. Before applying the two compiled runtime modules (`esp32-serial.js`, `ips10-roster.js`, plus their available maps/declarations), the installed serial module was byte-identical to this checkout's unchanged pre-edit build (SHA256 `fc54d54147a2740f7ab7a376ccb6e2e6a41e183861fa925fd80bbff54fc356e8`). Previous files were backed up under ignored diagnostics. Supported `agentdeck daemon restart` brought up launchd-supervised PID 32048 with build identity `daf0a303c681`. The installed module covered all 25 fixture sessions in three bounded pages without injecting fixture data into the live daemon. This is a local module update, not a published npm release or an App Store deployment. A future package reinstall must include the committed roster changes to retain the enhancement.

Final IPS10 USB deployment verified the P4 MAC `80:f1:b2:d0:b4:bb` and 16 MiB flash before writing. Firmware SHA256 is `30ed6557e42f9c73a6d39697b04d0eea668b97ecf444796317ba691f4d41f0f9`, build epoch `1790171001`, pre-commit label `14ed7113-dirty`, UI `aquarium-v5`. Application usage is 5,972,192 / 6,291,456 bytes (94.9%). The 90-second read-only observation confirmed three live sessions, two projects, four visible quota windows and 219 advancing UI updates. Sampled widget-update median was 15.3 ms (startup-inclusive maximum 86.9 ms); these timings exclude display flush and are not FPS. After startup, free internal heap stayed 47–48 KiB; minimum-ever reached 39 KiB and largest free blocks ranged 3–6 KiB, so this short observation does not establish long-duration fragmentation or voice-load headroom. No reset occurred in the observation. The serial lease was resumed and daemon health confirmed connected, fresh/non-stale device information at uptime 98 seconds with the same new build epoch.

The cross-board check caught IPS10-only field references in shared reset and simulator fixture code; both are now guarded by `BOARD_IPS10`. IPS35 native build/render and the final IPS10 interaction run passed. These guards do not change IPS10's preprocessed executable code. Physical evidence is runtime telemetry; visual inspection used the actual firmware widgets rendered with fixture data, not a photograph of the panel. Speech recognition quality and front-camera vision remain deferred, and no npm/App Store release was made.
