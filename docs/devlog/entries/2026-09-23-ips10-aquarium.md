# 2026-09-23 — IPS10 English aquarium and conditional quota rings

Replaced the project workbench with an underwater ambient dashboard using the existing AgentDeck mark, canonical agent creatures, and IBM Plex Sans KR Bold. Dashboard labels are English; received project names, questions and activities retain their original language. The generated background is an empty environment, not a baked dashboard: live creatures, state badges, activity, attention and gauges remain real LVGL widgets. The source PNG, generation prompt and deterministic RGB565 conversion are retained under `design/ips10/`.

Claude, Codex and z.ai usage now uses circular used-percentage gauges. Unknown windows, empty reset labels and inactive providers disappear; the full rail collapses when nothing is known. Zero remains valid, stale Claude does not hide Codex, MCP is labeled separately, and Antigravity credits never become percentages. Three or four providers reduce gauge size to keep the complete rail visible. Empty activity/event/token/cost fields and inactive voice status are omitted.

Three project lanes (two in the portrait preview) rotate every 30 seconds, with an optional pause. Each lane rotates up to three identity-ordered agents and their activities every 12 seconds. Global attention remains visible for off-page projects. Pools stay fixed at ten received sessions regardless of the reported total. The current host serial transport still caps detail at ten prioritized sessions; the UI explicitly says `10 of N agents · Priority view`. This does not implement host-side browsing of every session in a larger roster. Same-project grouping indicates co-presence, not inferred delegation.

Native interaction checks pass in landscape and portrait, including ten projects, ten peers, visibility over the full cohort cycle, a reported total of 1,000, missing/zero/stale quota, four providers, credits bounds, selection, strict event attribution and voice controls. Physical rotation remains unsupported and microphone recognition revalidation remains deferred. Build/typecheck passed. The full JavaScript suite initially passed 4,653 tests with one SSE display-state timing failure; the unchanged integration file then passed all 16 tests in an isolated retry. Protocol generation has no drift; all seven token mirrors, documentation/catalog checks and PNG conversion verification pass. Design lint remains at the existing 92 violations.

Two full USB deployments verified the identified P4 / 16MB device and flash hashes; the second includes the final long-project-name clipping correction. Final image SHA256: `3abf74514b930cc741ae7ec1ee2a58e061cf1202d60b29fc70f580afe163603a` (6,032,944 bytes). Application section: 5,968,540 / 6,291,456 bytes (94.9%); static RAM: 89,684 bytes. Installed identity: `7f66df4b-dirty`, epoch `1790162376`, UI marker `aquarium-v4`. The 90-second observation confirmed three live sessions/two projects and 234 advancing updates without observed panic/reboot. Internal free heap settled at 60–61 KiB, minimum-ever 57 KiB, largest block 17–19 KiB. Median state-to-widget processing was 6.278ms; startup-inclusive maximum 66.439ms. These timings exclude the display flush and are not FPS measurements. After releasing the serial lease, the existing daemon confirmed a fresh, connected, non-stale device at uptime 100s with that exact epoch. The newer installed daemon was preserved.
