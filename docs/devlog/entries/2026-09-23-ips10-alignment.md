# 2026-09-23 — IPS10 shared grid, typography hierarchy and baked creature reliefs

The user identified inconsistent alignment and unclear content hierarchy despite the earlier quota polish. Compared the native Apple `ProviderRow` leading alignment and Android `MonitorLayoutScale` role/spacing model, then rebuilt the IPS10 arrangement around a documented grid rather than activity-weighted column widths: 24px outer margins, equal project tracks, 16px gutters/insets and a shared project/quota heading baseline. Each agent owns a 120px row pairing its illustration with state, activity and reported workers. Wide single-project cards use equal horizontal peer tracks; narrower cards stack rows. Results follow the visible peer region. Background contrast is reduced and grouped card surfaces identify content ownership. Footer copy has a real bottom inset.

Typography now separates 20px bold headings from 20px Regular activity, with 16px Regular metadata replacing 12px labels. Wordmark and quota values retain 28px bold; existing detail numerals retain 36px. Added a 2bpp IBM Plex Sans KR Regular ASCII face with the existing Korean fallback. The exact roles and geometry are recorded in `design/ips10/README.md`; English labels, received-language content, conditional Usage, plan-only Antigravity and bounded roster paging are preserved.

Blender MCP was unavailable, so the installed Blender 5.2 CLI baked three 112px shallow reliefs from the existing canonical Claude/Codex/OpenClaw alpha masks and token colors. This adds depth without redrawing the marks or introducing a live 3D engine. Three shared RGB565+A8 flash images total 110.25 KiB; other agents retain their canonical glyphs. `encode_creatures.py --check` verifies the PNG-to-consumer bytes. Removed the now-redundant per-agent icon and branch-line objects. Assets and font wrappers are IPS10-only.

Validation: build/typecheck and all 306 JS test files passed (4,657 tests, two skipped); protocol generation has no drift, seven token mirrors pass, and lint retains 92 source findings (40 additional findings come from ignored artifacts). Native landscape and portrait interaction checks pass, including title baselines, quota bounds, missing/zero/stale data and peer placement. Visual review caught a transient clipped peer when a project widened: LVGL still reported the previous width during the same update. Placement now uses the newly computed track width, and regression checks require all three peers to fit inside their card and share the wide-layout row. The final renders were re-inspected after this correction.

Deployment: a full USB write verified the board identity and brought up the revised widgets; the final wide-card/first-frame correction was then installed through the supported WiFi OTA path (6,012 chunks). Final image SHA256 `9d490d35c43962ba3d78a31964f248c2174ff9ee94741a8ea11120bb19b598ce`, pre-commit label `bb9d1ba9-dirty`, build epoch `1790174651`, UI `aquarium-v7`. Application size is 6,091,054 / 6,291,456 bytes (96.8%). Read-only 90-second serial observation confirmed three sessions, two projects, three quota windows and advancing UI updates in both overview and detail modes; Usage stayed visible. Internal free heap was 54–56 KiB, largest block 11–14 KiB and minimum-ever 50 KiB. Sampled widget-update median was 16.4 ms, startup-inclusive maximum 80.3 ms (not display FPS). No reset occurred during the observation. The serial lease was resumed and daemon health confirmed fresh/non-stale final-build data. Physical evidence is telemetry; supplied images are actual firmware widget renders with fixture data, not photographs.
