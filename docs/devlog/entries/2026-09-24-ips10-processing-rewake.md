# 2026-09-24 — Re-wake IPS10 while an answer is pending

The user could not invoke OpenClaw again during Processing and reported unreliable spoken replies. Firmware explicitly suppressed wake detection in both Transcribing and Waiting, leaving a lost reply uninterruptible for up to eleven minutes. It now listens for an explicit new wake during those phases, while retaining suppression during playback and ownership of the upload buffer. Re-wake starts a new recording; it supersedes the old device reply, not an already-running agent tool operation.

A boot-randomized uint32 request generation travels with the HTTP recording and returns on transcript, reply-ready and terminal notices. IPS10 ignores mismatched replies. Host supersession starts before recognition, is checked after recognition and gateway acknowledgement, and remains checked through asynchronous synthesis so an older completion cannot stage audio over a newer response. Older clients without an identifier remain supported. Device info exposes the current identifier for deterministic hardware diagnostics.

HTTP-pull notifications now resolve the current transport after synthesis, sharing the router's existing streamed-audio reconnect logic. Synthesis failure, skipped speech, failed download and playback task failure leave the pending state instead of looking indefinitely busy. These are identified failure paths, not proof that every reported playback failure had the same cause.

Validation: IPS10 builds; protocol generation has no drift; JavaScript build/typecheck and 4,676 tests pass (2 skipped), including pull-transport reconnection regressions. Token mirrors pass. Design lint retains 171 existing findings. Main source is committed as `62e9088e`; installed host reports build `af22c164b7fb`. USB compressed full-image write and flash hash verification passed. Serial readback reports compiled identity `8cc57ef7-dirty`, build epoch `1790217745`, firmware SHA256 `619b09445416d52a6623a4b784c03036074bc5d65bc75094617efced0f767816`.

Hardware regression passed: injected a delivered result to enter Waiting, acoustically invoked OpenClaw with a harmless command, observed a new request generation and Listening, then injected both an old-generation failure and old-generation audio-ready notice. The new recording remained active. Its response downloaded and played all 39,646 / 39,646 PCM bytes with zero dropped frames, returning to wake. A separate skipped-reply notice moved Waiting to the recoverable error state (wake detection remains enabled). Diagnostics: `diagnostics/voice-reentry/hardware.log`. Human speech accuracy and playback in the user’s own conditions still require listening validation; prior wake-only false submissions are not claimed fixed here.
