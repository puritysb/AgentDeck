// Unity-include wrapper for the 16px Noto Sans KR LVGL font. The firmware-side
// wrapper carries the BOARD_IPS10 guard, so this compiles to nothing for every
// other sim env — same per-env rationale as the rest of fw/.
#include "../../../src/ui/fonts/font_noto_kr_16.c"
