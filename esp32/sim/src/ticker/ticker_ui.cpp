// Unity-include wrapper for the T-Display-S3-Pro "Focus Strip" render tree.
// Same rationale as src/fw/renderer.cpp — pass-through only, so the firmware
// stays the single copy of the UI and it compiles with this env's defines.
#include "../../../src/ui/ticker/ticker_ui.cpp"
